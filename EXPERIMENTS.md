# Experiment registry

All numbers must be classified as `PUBLISHED_EXTERNAL_RESULT`, `LOCAL_BASELINE`, `LOCAL_MEASUREMENT`, `MODELLED_ESTIMATE`, or `STRETCH_TARGET`.

| ID | Hypothesis | Status | Owner | Gate / rollback |
| --- | --- | --- | --- | --- |
| E053 | Implement the first SM120a fused historical OSCAR INT2 attention path | accepted locally | oscar/performance/kernel | Added isolated `OscarInt2G128` historical-only CUDA score/softmax/AV launches for D=256, Q/KV=24/4, GQA=6, FP32 metadata, and 30K-calibrated coordinate inputs. Packed K/V are decoded directly into FP32 QK/AV accumulators; no persistent decoded K/V buffer was allocated and no prefix/recent or runtime integration was changed. Independent CPU-oracle parity passed at 128/512/2K/4K with worst relative-L2 `1.105956699e-06` (softmax) and `1.089803277e-06` (AV). RTX 5090 `sm_120a` benchmarks through 32K were `0.056451/0.161510/0.401674/0.801082/1.444493/2.968627/6.012608 ms` for 128/512/2K/4K/8K/16K/32K, or `119.0–301.7x` versus the CPU scalar historical oracle. D4.2a PASS; authorize D4.2b mixed prefix/recent integration. Evidence: `results/oscar/phase-d4-2a-fused-history.md` |
| E054 | Integrate the qualified historical kernel with BF16 prefix/recent for complete mixed OSCAR attention | accepted locally | oscar/performance/kernel | Added isolated mixed-tier SM120a score/softmax/AV launches for BF16 prefix + official `OscarInt2G128` historical + BF16 recent. One global FP32 softmax spans all tiers; historical K/V decode is direct with no BF16 shadow. Cache-backed boundary/aging/partial-page tests, D2.3 scalar-reader taps, forced contexts 512–519, layers 3/35/63, and all 16 full-attention dispatch bits passed; required-case worst AV relative-L2 was `5.148370292e-06`. Complete mixed latency was `0.208710/0.667861/1.109395/2.153588/4.373304/9.361144 ms` at 512/2K/4K/8K/16K/32K, or `213.2–272.3x` versus the scalar mixed baseline. D4.2b PASS; authorize D4.3 real Qwen runtime integration. Evidence: `results/oscar/phase-d4-2b-mixed-gpu-attention.md` |
| E055 | Connect the qualified mixed OSCAR GPU reader to the real Qwen3.8 runtime | accepted locally | oscar/performance/live-runtime | Added explicit `oscar-int2-gpu` mode using actual runtime post-RoPE Q/K/V, validated C4 rotations, typed BF16-prefix/official INT2-history/BF16-recent pages, D4.2b GPU mixed attention, and FP32 `R_V.T`. Live/reference taps at 321/332/512/4K passed at layers 3/35/63 with worst rotated-AV/recovered relative-L2 `2.52443e-6/2.50990e-6`; all 16 full-attention bits were `1111111111111111`, with zero GDN, legacy-Q2, BF16-history-shadow, fallback, and corruption dispatch. Real verifier wall was `2.903730/2.776749/6.889555/486.346291 s` at 321/332/512/4K; 4K cumulative profile was QKV rotation `0.590442 s`, append/aging `398.977 s`, staging `0.132383 s`, fused mixed GPU `46.0793 s`, recovery `0.339310 s`, full attention `480.292 s`. A four-case D3.1-style GPU smoke including a 1,148-token retrieval prompt achieved 4/4 objective success. A 16K exploratory run reached logical context 3,072 without correctness failure but was stopped because first-integration scaling was impractical; 32K was not attempted. D4.3 PASS for live correctness; authorize D4.4 persistent GPU-resident incremental cache/aging optimization. Evidence: `results/oscar/phase-d4-3-live-gpu-runtime.md` |
| `E000` | Native Windows build can be reproduced from the relocated checkout with local dependencies | accepted | integrator | CMake Release build + exact 94-entry CTest (`0` failures, `5` expected skips); rollback is source commit `3e2a28be` |
| `E001` | Correct root/scaffolding and artifact hashes make subsequent measurements restartable | accepted | integrator | `PROJECT_STATE.md` + baseline JSON + clean git state |
| `E002` | Windows-aware serving/benchmark defaults remove path-only smoke failures without changing runtime semantics | accepted | Windows/benchmark | Python compile/import checks, benchmark unit test, and native Windows binary-path resolution passed; Linux paths remain unchanged |
| `E003` | Canonical dev StateImage/paged-KV/Engine cutover can be integrated as one reviewable Windows worktree | accepted locally | upstream/runtime | staged integration tree builds; 94-test CTest passes with 5 expected skips; Qwen3.8 NVFP4 resource-settlement route passes; retain `9a6813a3` as rollback until review/commit |
| `E004` | NInfer parser drift fixtures improve agent tool fidelity without changing canonical templates | pending | serving | streaming/differential/security corpus; stop after three attempts |
| `E005` | The Qwen3.8 NVFP4 artifact supports native-Windows text and Vision serving through the staged Engine/resource tree | accepted locally | Windows/benchmark | separate text-only and `--vision` contract smokes pass; reports under `profiles/bench/qwen38-nvfp4-{text,vision}-smoke-20260826` |
| `E006` | The native Qwen3.8 NVFP4 path reaches the published-style 252,928/262,144 context attempt with FP8 KV without OOM | accepted locally | benchmarking | 252,928 and 262,144 token synthetic prefill complete; capacity/prefill only, not quality or C2/C4/C8 evidence |
| `E007` | The current three-coefficient transfer roofline is stable enough to publish a machine preset | rejected | context-cost | two transfer runs rejected D2H/D2D/H2D held-out p95 gates; keep compiled conservative defaults and improve model/fixture before retry |
| `E008` | The Qwen3.8 NVFP4 serving corpus exposes reproducible long-context, reasoning, code, translation, story, and structured-output behavior under MTP0/MTP3 | accepted locally | benchmarking | MTP0 20/20 and MTP3 75/75 formal requests complete with request/log counter checks; reports under `profiles/bench/qwen38-serve-corpus-20260827*`; outputs are measurements, not a quality gate |
| `E009` | Extending the fused NVFP4 SwiGLU W4A4 route from T=48 to T=64 improves a ragged prefill boundary | rejected | nvfp4-mlp | Focused correctness passed, and the isolated T=64 microbench improved `138.048`→`70.272` µs with Nsight Systems confirming two launches instead of three; real Qwen3.8 MTP3 saturation was neutral at C=1 (`168.7`→`169.2` tok/s) and slightly negative at C=4 (`544.2`→`542.8` tok/s), so the schedule was rolled back |
| `E010` | DFlash2 direct Q/K/V projection is faster than materializing packed QKV and copying three slices | accepted locally | dflash2 | Exact focused comparison and guard checks pass; repeated `T=8`, W8G32 `6144x5120` samples show about 19–21% lower latency for direct outputs; no E2E artifact claim |
| `E011` | DFlash2 top-16 selector throughput improves with packed survivor keys and a 512-thread warp-shuffle reduction | accepted locally | dflash2 | Focused selector/path correctness passes, including same-thread lower-ID ties; `248,320 x 8` synthetic selector work is `327.7` µs versus `565–570` µs for the prior 128-thread route, about 42% lower. The final Nsight Systems capture reports the selector as 79% of focused GPU time, with a 313.8 µs median kernel instance |
| `E012` | Warp-paired DFlash2 dynamic convolution improves the grouped two-tap kernel | rejected | dflash2 | Correctness passed, but the paired warp mapping measured about `6.87` µs versus `6.73` µs for the retained 256-thread route; the original mapping remains |
| `E013` | Protected NVFP4 DFlash2 KV should preserve fixed prefix sinks/pivots and recent tokens without forcing the full local cache to BF16 | accepted locally | vericache | BF16 sidecar append, copy, StateImage, and direct-attention tests pass for combined anchor/recent mapping; anchor-only layout planning also passes. The matched 5090 row measured `27.628` ms with 8 anchors + 64 recent versus `28.3265` ms control, but acceptance was `10/56` versus `11/56`; retain as a quality-protection path with no speedup claim |
| `E014` | An adaptive-boundary StateImage promotion can populate pinned L1/L2 residency without blocking the DFlash2 round | accepted locally | vericache | One asynchronous host snapshot completed on the 5090 with `167,389,184` bytes and `L0/L1/L2/L3 = 26,869,760/13,434,880/153,954,304/0`; host-tier logits were not independently verified, so this is a checkpoint/residency milestone, not exact VeriCache |
| `E015` | Typed host-KV COW plus StateImage promotion can rotate through multiple L1→L2 boundaries and expose real PCIe accounting | accepted locally | vericache | Clean RTX 5090 DFlash2/NVFP4 stress run at context 512, `k=7`, 400 rounds, fixed `l1=256`: three checkpoints, 33 KV pages, 71,565,312 KV D2H bytes, 502,167,552 StateImage D2H bytes, and `L0/L1/L2/L3 = 26,869,760/17,367,040/221,063,168/0`; GPU round was 1.32% below control but published throughput was lower due acceptance, so retain as research infrastructure rather than a winning configuration |
| `E016` | The same hierarchical control path can be probed from native MTP with VeriCache-NVFP4 | pending | vericache/MTP | The new MTP harness compiles and exposes hierarchy/host-transfer telemetry, but BF16 and VeriCache-NVFP4 probes stalled after model allocation without producing a round; no physical MTP result is accepted until startup is diagnosed |
| `E017` | Native serving can use the hierarchical VeriCache + DFlash2 profile by default without breaking startup or the HTTP contract | accepted locally | serving/vericache | Serving defaults now resolve to VeriCache-NVFP4, DFlash2 `k=7`, optimized proposal head, and hierarchical host snapshots; `--no-spec --no-hierarchical-vericache` restores the stable route. A guarded RTX 5090 smoke loaded the DFlash2 artifact in 49.2 s, returned `/health`=`ok`, and completed Chat Completions. This is startup/API evidence only; host-tier verifier equality and workload-level speed remain unqualified |
| `E018` | Event-ordered hierarchical host promotion can overlap StateImage/Host-KV DMA with the next decode work while preserving concurrent-lane ownership | accepted locally | vericache/overlap | Focused hierarchical, StateImage, KV-append, and context-store tests pass. Matched physical RTX 5090 DFlash2/NVFP4 runs at context 512, batch 2, `k=7`, optimized head, no graph, one warmup, 100 rounds measured `23.6491` ms GPU / `23.6622` ms wall and `182.992` aggregate tok/s with async host snapshots versus `23.6857` / `23.6995` ms and `186.502` tok/s control; acceptance was `16.64%` versus `17.29%`, so no end-to-end speedup is claimed. Two concurrent checkpoints completed with `334,778,368` StateImage D2H bytes, `71,303,168` KV bytes across 32 pages, and `0.148862` s measured KV-D2H latency. Reservations publish only after the completion event; rollback cleanup drains DMA before unpinning. |

| `E019` | DFlash2 selector working-set trimming and single-lane path tracing reduce decode-side temporary work without changing the selector contract | accepted locally | dflash2 | Exact-width lattice and path-trace correctness pass with guard checks. The production FP32 lattice shrinks from hidden-width 5,120 to semantic width 272 (94.7% less lattice storage per token), and the deterministic trace launch drops from 32 threads to the only participating lane. A matched context-512 RTX 5090 eager pair measured `21.7017` ms GPU / `21.7124` ms wall for hierarchy enabled versus `21.7685` / `21.7784` ms control; published rates were acceptance-sensitive (`96.0281` versus `79.4367` tok/s), so this is retained as a low-risk working-set/launch optimization with no standalone throughput claim |
| `E020` | The graph-safe DFlash2 selector enables CUDA Graph capture of the full speculative round | accepted locally | dflash2/serving | Focused DFlash2 and hierarchical tests pass. On the physical RTX 5090 with context 512, `k=7`, optimized head, VeriCache-NVFP4, host snapshots, and one warmup: C=1 graph/eager measured `19.6386`/`21.3438` ms GPU and `102.306`/`94.1346` published tok/s with identical `101/700` acceptance; C=2 measured `21.8301`/`23.6388` ms and `201.013`/`184.364` tok/s with `239/1,400` versus `236/1,400` acceptance. The default server smoke passed the full text HTTP contract and reported DFlash2, hierarchy, host snapshots, and the `nvfp4-dflash2` cost profile |
| `E021` | Hierarchical host checkpoints need an immutable KV-address manifest aligned with the immutable GDN StateImage before a later checkpoint can replace them | accepted locally | vericache/ownership | The manifest now retains the inactive COW KV address spaces, records the frozen StateImage content epoch, and validates host replica currency, committed coverage, frontier/page alignment, and extent ownership before publication. Focused DFlash2/hierarchy tests pass. A guarded RTX 5090 run at context 1,024, C=1, graph mode, `k=7`, and 300 measured rounds crossed two L1→L2 boundaries at `22.1042` ms GPU / `22.1153` ms wall and `89.9829` tok/s; two host snapshots completed (`334,778,368` StateImage D2H bytes and `111,149,056` KV D2H bytes across 49 pages). This establishes checkpoint ownership/replacement, not independent host-tier logits or a speedup claim |

| `E022` | Fusing the Qwen3.8 W8G32 gate/up projection with SiLU-multiply removes DFlash2 v2's large BF16 gate/up temporary and activation launch | accepted locally | dflash2/mlp | Qwen3.8 W8_A16 correctness passes for T=1,2,8,16,32,40,41,64, including both the latency-oriented small-T schedule and the predicated large-T route. A guarded RTX 5090 C8 run with VeriCache-NVFP4, hierarchical host snapshots, CUDA Graphs, and `k=7` measured `34.2246` ms GPU / `34.2426` ms wall, `511.06` published tok/s, and `475/2,800` accepted draft tokens. This is a retained default-path reduction in intermediate traffic; acceptance variance prevents an end-to-end speedup claim |
| `E023` | Make direct DFlash2 Q/K/V projection reuse the existing W8G32 Tensor Core small-T and row-split MMA routes instead of the warp-per-row SIMT kernel | accepted locally | dflash2/qkv | Exact focused comparison and guard checks pass for `T=1,8,16,48,49,64`. Direct split-output timing is `14.42` µs versus `45.12` µs for packed-GEMM-plus-copy at `T=8`, and `106.96` versus `112.06` µs at `T=64`. Guarded hierarchical VeriCache + CUDA Graph RTX 5090 runs at context 512 measured C1/C2/C4/C8 wall rounds of `19.6804/21.6838/26.1826/33.1707` ms and a repeat C8 of `33.2945` ms. Acceptance varied across runs, so the raw end-to-end improvement is recorded without claiming a standalone throughput multiplier |
| `E024` | Keep the cheap private Qwen3.8 proposal head while restoring target-head unary fidelity with sparse FP8 candidate rescoring | accepted locally | dflash2/proposal-head | Focused FP8 candidate-scoring correctness and guard checks pass. On the physical RTX 5090 with the same context-512, VeriCache-NVFP4, hierarchical host snapshots, CUDA Graph, `k=7`, warmup 2, and 50-round setup, optimized private-head + sparse target-row rescoring versus full-head control measured C1 `19.1339`/`19.7155` ms wall and `104.526`/`92.3131` published tok/s with `50/350` versus `41/350` accepted drafts; C8 measured `32.8849`/`33.3314` ms and `541.891`/`522.631` tok/s with `491/2,800` versus `471/2,800` accepted drafts. This is a retained proposal-path result; target verification remains authoritative and broader quality/workload qualification is still pending |
| `E025` | Trim optimized DFlash2 proposal workspace and retain a wider private shortlist as an opt-in recall experiment | accepted locally | dflash2/proposal-head | Focused selector, sparse-rescore, reduction, and guard tests pass. Optimized planning now sizes the private proposal logits to the artifact's 131,072 draft rows instead of the 248,320-row target vocabulary. The default private-16 route measured C1 `19.1114` ms / `104.601` tok/s and C8 `32.8525` ms / `544.038` tok/s in the current RTX 5090 samples. `NINFER_DFLASH2_WIDE_SHORTLIST=1` keeps a 32-row high-recall tail available, but its matched C8 sample was `33.054` ms / `534.108` tok/s with `483/2,800` accepted drafts, so it is not the serving default |
| `E026` | Default pairwise NVFP4 decode for dense and DFlash2 local attention while retaining scalar-order behavior in the DFlash2 proposer | accepted locally | vericache/attention | Focused dense and sliding-window correctness suites pass in both default and `NINFER_NVFP4_PAIR=0` fallback modes. The dense cached operator measured roughly 9–10% lower 8K latency with pair decode than the scalar route. Fresh Qwen3.8 DFlash2/VeriCache RTX 5090 samples at context 512 measured default versus scalar C1 `18.6615`/`18.8937` ms wall and `192.911`/`190.540` tok/s with identical `13/35` acceptance; C8 measured `32.3135`/`32.7918` ms and `736.535`/`731.891` tok/s with `79/280` versus `80/280` accepted drafts. Acceptance variance prevents claiming a universal end-to-end multiplier; `NINFER_NVFP4_XQA=1/2` remains opt-in because the grouped XQA prototypes were slower on the same GPU |
| `E027` | Explore wider Q4 small-T schedules for the Qwen3.8 DFlash2 private proposal head without disturbing the tuned serving default | accepted locally | dflash2/proposal-head | Focused Q4 correctness and guard tests pass. On a Qwen3.8-shaped `131,072 x 5,120` synthetic head, the opt-in K-wide eight-warp schedule was about 1.6–2.3% faster at T=6/T=8 but slower at T=2/T=4; a guarded physical DFlash2 round was neutral/slightly slower. `NINFER_Q4_DRAFT_HEAD_WIDE=1/2` remain opt-in research controls |
| `E028` | Explore wider W8 gate-up schedules for the Qwen3.8 DFlash2 proposal path without disturbing the tuned serving default | accepted locally | dflash2/proposal-head | Focused Qwen W8 LinearSwiGLU correctness passes for the production, eight-warp, eight-warp/min-blocks-one, and sixteen-warp candidates. On physical RTX 5090 DFlash2 rounds, the eight-warp variants were mixed and acceptance-sensitive; the opt-in large-T BM64 route was slower at C8 (`749.75` vs `808.09` tok/s in matched runs). The production narrow schedule remains default, while `NINFER_DFLASH2_W8_GATEUP_WARPS=8/81/16` and `NINFER_DFLASH2_W8_GATEUP_LARGE_SCHEDULE=64` remain available for further workload-specific tuning |

| `E029` | Fuse the exact Qwen3.8 FP8 target vocabulary projection with greedy argmax and use token-only acceptance when sampling is greedy | accepted locally | dflash2/target-head | Focused FP8 A16 linear and speculative-round correctness passes, including BF16-rounding and lower-ID tie semantics. The fused route avoids materializing/re-reading the `[248,320, T]` target-logit matrix and reuses the existing target-logit buffer for compact row-tile winners. A physical RTX 5090 uncaptured C1 A/B was GPU-neutral (`20.4853` ms full versus `20.4424` ms fused); acceptance differed (`18/56` versus `14/56`), so no throughput gain is claimed |
| `E030` | Capture the fused greedy FP8 target-head verifier as a graph-safe C1 serving family | accepted locally | dflash2/target-head/graphs | Added a separate greedy C1 graph family with fixed scratch/reduction topology; sampled requests and C2/C4/C8 continue to use the full-logit graph family. Focused linear, speculative-round, and DFlash2 suites pass. On the RTX 5090, matched context-512 hierarchical/host-snapshot/CUDA-Graph runs measured full versus fused GPU rounds of `18.6411`/`18.6593` ms and wall rounds of `18.6549`/`18.6693` ms with identical `2.8` licensed tokens/round and `18/70` accepted draft tokens. The fused graph is therefore retained for exactness and working-set reduction, but is neutral/slightly slower and carries no throughput claim |
| `E031` | Separate asynchronous host-checkpoint cadence from the adaptive host-verifier horizon and keep repeated COW manifests valid after Host extent partitioning | accepted locally | vericache/checkpoints | Added `host_snapshot_horizon=0` (follow adaptive L1→L2) plus a serving default of `2048`; the 32K/C=1 DFlash2 stress path at `k=7`, optimized head, VeriCache-NVFP4, CUDA Graphs, and 600 rounds now completes two repeated snapshots without manifest failure. The fixed-2048 run measured `55.9164` ms GPU / `55.9287` ms wall, `27.7139` tok/s, and `330/4,200` accepted drafts; the matched fixed-512 control was `55.9827` / `55.9933` ms, `27.6819` tok/s, and the same acceptance. Both published two snapshots in this long run, so no standalone throughput or transfer reduction is claimed; the manifest refresh is a correctness/ownership fix |
| `E032` | Make the live OSCAR-Q4 verifier the default serving proposal while retaining OSCAR-Q2 as a dual-written resident fallback | accepted locally | vericache/dflash2 | Added an explicit Q4-primary route and a benchmark-only `--vericache-q2-filter` two-pass control. On the physical RTX 5090 at context 512, `k=7`, optimized head, CUDA Graphs, host snapshots, and five measured rounds, Q4-first measured `18.4731` ms wall / `184.052` published tok/s with `12/35` accepted drafts; Q2→Q4 measured `21.6515` ms / `157.033` tok/s with the same final `12/35` acceptance and `22/49` Q2→Q4 common-prefix acceptance. The historical Q4/NVFP4-style sample was `190.540` tok/s at `18.8937` ms with `13/35`; the remaining gap is acceptance/calibration variance. L2 host KV is now stored as FP16 records (device target KV remains BF16 for kernel compatibility), and focused OSCAR/hierarchy tests pass |

| `E033` | Make OSCAR storage layout an explicit shared contract before calibrated assets are introduced | accepted locally | oscar/fidelity | Added typed `OscarKVLayout` propagation through paged geometry/views, full append, dense prompt/decode, and StateImage conversion; added QKV dump manifest/hash validation. Designated Debug build and focused OSCAR/runtime/StateImage tests pass. This is a safety and provenance milestone only: no production OSCAR INT2 or calibrated-quality claim is made while captures/assets/fitter remain absent |

| E034 | Establish the offline OSCAR calibration environment and verify the exact Qwen3.8 full-attention capture boundary | accepted locally | oscar/fidelity | Python 3.12.10 isolated CPU venv at D:\AI\tools\oscar-calibration\.venv with torch 2.13.0+cpu passed import, deterministic .pt round-trip, and deterministic 128x128 eigh checks. Latest nvfp4-dflash2 artifact metadata and DFlash2 load-plan passed; runtime/artifact topology is 64 layers, 16 full layers (3,7,...,63), 48 GDN layers, Q/KV 24/4, D/R 256/64, with candidate source qn/kn/v after RoPE and before cache append. No QKV capture or calibration was run |

| E035 | Implement and validate a deterministic OSCAR-compatible 256-token native Q/K/V smoke capture at the verified full-attention boundary | accepted locally | oscar/fidelity | Focused target `ninfer_qwen3_6_27b_oscar_capture_test` in `D:\AI\build-adaptive-dflash2` passed with a 256-token deterministic prompt and one greedy output token. All 16 full-attention layers (3,7,...,63) produced aligned BF16 Q/K/V: Q `[256,24,256]`, K/V `[256,4,256]`; the fail-closed validator passed payload hashes, manifest sidecar hash, finite-value checks, exact topology/GQA checks, and representative layer values. Raw payload bytes: `67,108,864`; manifest SHA-256: `df0319563e91cc71a6dcc16a5f34f167da47931566f4ae94fc25f246a8425a89`. No GDN, drafter KV, fitting, 10K/30K capture, INT2 integration, or kernel optimization was performed |

| E036 | Convert the validated 256-token smoke dump and run the unchanged official OSCAR `qqt_sst` + `r_h_pbr` fitter | accepted locally | oscar/fidelity | Converter validated the original manifest, preserved all BF16 bit patterns through `.pt` save/reload, emitted 48 output hashes, and used explicit chunk `1.pt` because upstream `--chunk-id all` skips chunk 0. Official FutureMLS-Lab/OSCAR main commit `41ebcdba3db5f0ce1339c3727caea80df575d437` (script SHA-256 `f7f9f738be5dea75cc1d7e8d6928e4ee91df1cbf1e1c7b7d9c7d308b55d4da8b`) completed `qqt_sst` + `r_h_pbr` with `head_dim=256` in 2.015s. Separate K/V FP32 `[256,256]` assets for all 16 layers passed deterministic reload, finite-value, exact mapping, per-layer orthogonality, and independent 8x8 R*H*Pbr fixture checks. Worst errors: K `2.011652722e-08`, V `1.916613712e-08`; no runtime integration or production calibration claim |

| E037 | Prove fitted OSCAR rotated-BF16 attention invariance before INT2 integration | accepted locally | oscar/fidelity | Offline reference test passed for all 16 full-attention layers using `Q'=Q*R_K`, `K'=K*R_K`, `V'=V*R_V`, GQA mapping `q_head//6`, and post-attention `R_V.T`. Max score error `4.971724872e-07`, softmax error `7.114301309e-08`, recovered post-attention error `3.373735723e-06`; the raw V-rotated output was intentionally non-invariant, reaching `82.3280771`, confirming inverse-V placement. No orientation/RoPE/layer/GQA blocker was found; full layer residual/output projection remains outside the capture artifact |

| E038 | Prove the calibrated OSCAR INT2 reference chain before runtime integration | accepted locally | oscar/fidelity | Slow CPU reference ran layer 3 first and then all 16 full-attention layers over the 256-token smoke dump. It compared BF16, rotated BF16, existing fixed-Hadamard Q2, and calibrated `qqt_sst` + `r_h_pbr` INT2. Calibrated quantization matched pinned upstream `simulate_int2_asym` element-for-element; codes were finite and in `[0,3]`; inverse `R_V.T`, GQA, and layer mapping passed. Maximum calibrated score/softmax/attention-output errors were `7.058641e+00`/`8.521844e-01`/`4.403106e+01`, versus fixed-control output max `1.280092e+02`; classified PASS for mathematical reference coherence only. Full layer output/LM logits, 10K/30K quality, CUDA packing, and runtime integration remain unqualified |

| E039 | Produce and qualify a 10K OSCAR calibration pilot before 30K | accepted locally | oscar/fidelity | The unchanged full-attention capture boundary produced 40 independent 256-token chunks (10,240 useful tokens) for layers 3,7,...,63; a separate 1,024-token seed-1009 set was held out. Fail-closed raw and official .pt validation passed exact Q/K/V coverage, BF16 value preservation, hashes, finite values, and no GDN tensors. Official FutureMLS-Lab OSCAR main commit 41ebcdba3db5f0ce1339c3727caea80df575d437, qqt_sst + r_h_pbr, head_dim=256, chunks 1..40, completed for all layers. K/V FP32 assets passed deterministic reload and orthogonality (2.193682191e-08 K, 2.131072918e-08 V worst); held-out rotated-BF16 max score/softmax/output errors were 4.062135e-07/8.728284e-08/3.013387e-06. Calibrated 10K INT2 aggregate output max/relative-L2 improved over prior 256-token assets (37.105069/0.303087 versus 41.614878/0.317166), with layers 43/47/51 as largest output outliers. KEEP for 30K calibration only; group-128 runtime packing, output-projection/LM-logit quality, CUDA/runtime integration, and 30K remain unqualified. Evidence: results/oscar/phase-c3-cal10k.md and results/oscar/evaluations/phase-c3-cal10k-heldout.json |

| E040 | Produce and qualify the primary 30K OSCAR calibration asset set | accepted locally | oscar/fidelity | Immutable identity `qwen3.8-27b-oscar-qqt-sst-rhpbr-g128-cal30k-v1`; unchanged capture boundary; 120 independent 256-token chunks, 30,720 useful tokens, all 16 full-attention layers, and a separate 1,024-token held-out set. Raw/official validation passed complete Q/K/V coverage, BF16 preservation, hashes, finite values, exact topology/GQA, and no GDN records. Unchanged FutureMLS-Lab OSCAR main commit `41ebcdba3db5f0ce1339c3727caea80df575d437`, `qqt_sst` + `r_h_pbr`, D=256, official chunks 1..120, completed with exit 0. K/V FP32 assets passed deterministic reload, exact mapping, orthogonality (2.082505102e-08 K, 2.296734802e-08 V worst), and independent fixture. Held-out rotated-BF16 remained invariant; 30K INT2 improved aggregate output mean/relative-L2 over 10K (0.294053/0.302515 vs 0.295811/0.305705), effectively matched worst output max (39.595471 vs 39.497786), but regressed score/softmax maxima and argmax agreement. Qualified as a mixed-but-favorable production candidate ready for the next runtime-integration phase; group-128 packing/metadata and end-to-end runtime fidelity remain next-phase work. Evidence: results/oscar/phase-c4-cal30k.md, results/oscar/rotations/qwen3.8-27b-oscar-qqt-sst-rhpbr-g128-cal30k-v1/asset_manifest.json, and results/oscar/evaluations/qwen3.8-27b-oscar-qqt-sst-rhpbr-g128-cal30k-v1-heldout.json |

| E041 | Load the immutable C4 rotations into the real Qwen3.8 runtime and prove full-model rotated-BF16 equivalence | blocked | oscar/fidelity/runtime | The opt-in `oscar-rotated-bf16` loader and on-the-fly C1 Q/K/V + inverse-V path built in `D:\AI\build-adaptive-dflash2`; C4 identity, model/asset hashes, FP32 payloads, topology, 16-layer mapping, and telemetry passed. Deterministic 32-token A/B stopped at the first full-attention layer (3): Q/K score max `1.48105e-02`, softmax max `2.17747e-03`, recovered attention max `1.5625e-02`, post-MLP relative L2 `7.73924e-03`; final hidden relative L2 `5.98146e-02`, logits relative L2 `2.04136e-02`, top-1 and generated-token agreement both false. Pre-rotation Q/K/V were bit-identical and stage analysis found no orientation, RoPE, layer, GQA, or inverse-transpose mismatch; the blocker is BF16 round-off through the on-the-fly transforms/inverse. 512/4K were not run after the required stop. Evidence: `results\oscar\phase-d1-runtime-rotated-bf16.md` and `results\oscar\d1-runtime-diagnostics`. Do not begin INT2 or performance work until resolved |

| E042 | Isolate the minimum intermediate precision/fusion contract for real-runtime OSCAR rotations before INT2 | blocked | oscar/fidelity/runtime | D1.1 built four opt-in variants and ran the deterministic 32-token full-model check. FP32 rotation/reference attention/inverse reduced layer-3 Q/K/V orientation to relative L2 `3.209392e-7/3.082207e-7/2.861411e-7`, QK/softmax to `9.274475e-8/3.505521e-7`, and post-attention residual to `5.990686e-4`; FP32 inverse alone matched BF16 control. The best combined path still had hidden relative L2 `4.570910e-2`, logits `1.693360e-2`, false top-1 and generated-token agreement. Python/CUDA reference algebra was `1.091371e-6`, but the diagnostic reference attention is prefill-only and decode remains BF16-cache based. Recommended contract is FP32/fused online rotation, FP32 attention accumulation, FP32 inverse-V, one final conversion; matched prefill/decode runtime equivalence remains unresolved. Evidence: `results\oscar\phase-d1-1-rotation-precision.md`; do not begin D2 |

| E043 | Compare unrotated and OSCAR-rotated execution through a matched FP32 attention/cache path | blocked | oscar/fidelity/runtime | Added diagnostic-only `NINFER_OSCAR_MATCHED_FP32=1` control/candidate paths with identical FP32 causal GQA attention, FP32 persistent K/V cache through prefill and decode, and a common final BF16 boundary. The required 32-token prefill gate reached layer-3 FP32 recovered-attention relative L2 `1.368033032e-6`, but the common BF16 conversion produced post-attention residual `2.898554914e-5`, post-MLP/layer output `3.130950573e-4`, and final hidden/logit relative L2 `7.18921733e-2`/`2.32925045e-2`; top-1 and generated-token agreement were false. The first differing operation is the BF16 materialization before gate/output, with later decode A/B comparison confounded by different selected tokens. 512/4K were not run. No INT2, recalibration, DFlash/MTP, adaptive-K, or optimization work was performed. Evidence: `results\oscar\phase-d1-2-matched-attention-cache.md`; do not begin D2 |\n\nRejected/closed experiments must retain their manifests, evidence, and reason here. No optimization result is accepted without correctness and task-time evidence.
| E044 | Prove OSCAR rotation/cache coordinates across forced prefill-to-decode | accepted locally | oscar/fidelity/runtime | Added diagnostic-only forced ordinary decode for matched FP32 A/B. Prefill seed `997` and eight ordinary decode tokens `1001,1003,1005,1007,1009,1011,1013,1015` were committed identically. Layer 3 directly read back the persistent FP32 cache after every append: positions were identical/contiguous `0..39`, A read-back was exact, and maximum B-vs-A-transformed K/V errors were `3.08864e-7`/`2.86285e-7` relative L2. Maximum score/softmax/recovered-attention errors were `1.07778e-7`/`5.74594e-7`/`1.60696e-6`, below the `1e-5` gate with no growth. All 16 full-attention layers produced expected records at every step; no GDN records or production-cache fallback appeared. D1.3 PASS; exact natural full-model BF16 token equality is retired as this rotation-correctness gate and D2 may proceed under its own gates. Evidence: `results/oscar/phase-d1-3-forced-decode.md`, `results/oscar/phase-d1-3-validation-20260901e.json` |
| E045 | Prove official group-128 OSCAR INT2 quantizer/codec parity before live integration | accepted locally | oscar/fidelity/codec | Pinned FutureMLS-Lab/OSCAR serving source to commit `41ebcdba3db5f0ce1339c3727caea80df575d437`, clipped-INT2 source SHA-256 `c1d7fd911c688cf29df9b98ce19fb48c6e7147ea6fcc81761e33cbf5f38b4157`, and implemented distinct scalar `OscarInt2G128` semantics: K/V clips `.96/.92`, two independent g128 groups, FP32 scale/zero-point metadata, symbols `0..3`, and official `[j,j+64,j+128,j+192]` byte packing. Fifteen deterministic synthetic/real cases (63 rows) covered range/sign/clipping/group/page-boundary controls and rotated layer 3/35/63 K/V samples across all four KV heads. Compiled C++ parity and fail-closed hash validation passed exact clipped values, symbols, packed bytes, metadata, and decode; maximum reported parity errors were zero. The legacy experimental Q2 path was preserved separately. D2.1 PASS; live attention/mixed windows/throughput remain D2.2. Evidence: `results/oscar/phase-d2-1-int2-codec.md`, `results/oscar/d2-1-int2-fixtures/manifest.json`, `results/oscar/d2-1-int2-fixtures/cpp-parity.json` |
| E046 | Define and validate the typed mixed BF16/official OSCAR INT2 cache representation | accepted locally | oscar/fidelity/layout | Added `OscarMixedCacheBundle`/`OscarMixedLayerCache` with explicit BF16 prefix, official `OscarInt2G128` historical bulk, and BF16 recent page/slot storage. Page/slot metadata carries layer, sequence, logical/physical ranges, K/V types, OSCAR layout version, group size, and region role; pages never mix formats and the legacy Q2 path is unchanged. Static construction passed for 500 tokens across all 16 full-attention layers, including one-to-one addressing, `63/64`, unaligned `243/244`, final-position, K/V agreement, shared-policy, and no-GDN checks. Accounting covers contexts 64..4096 with full-page allocation, 56-byte headers, and 48-byte slot metadata; 500-token total is `23,328,768` bytes (`1.423875` bytes/value, `11.391` bits/value), with allocator bookkeeping explicitly excluded. D2.2a PASS; no aging/promotion, live attention, or optimization work. Evidence: `results/oscar/phase-d2-2a-mixed-layout.md`, `results/oscar/phase-d2-2a-layout-validation.json` (SHA-256 `fb158c93b4f9e5b6a9c1999392928c4318df3ec13674b572b245d7afb40a2ce7`) |
| E047 | Implement deterministic BF16-recent to official OSCAR INT2 token aging | accepted locally | oscar/fidelity/aging | Added `OscarMixedAgingCacheBundle` on the D2.2a typed layout. It validates the C4 asset identity `qwen3.8-27b-oscar-qqt-sst-rhpbr-g128-cal30k-v1`, model SHA-256 `6cc7560ae3427d8fa87b75c17e41328116b71b068c4c4dc06137fb73b656f64e`, runtime asset-manifest SHA-256 `4d6d7af496238c1c65c95cc9425f18c3d9cb6028d4dda42d4d0de91e9efaf560`, calibrated mode, and all 16 full-attention layers. Forced appends `0..323` kept prefix 0..63 BF16, token 64 recent BF16, then aged 64,65,66,67 on appends 320..323. Independent test-side D2.1 codec comparisons passed exact clipped values, FP32 metadata, symbols, and packed bytes for layers 3/35/63 and all four KV heads; all 16 layers executed four conversions. Bad asset, GDN, prefix-aging, double-conversion, overlap/hole, and BF16/historical residency guards passed. Final tiers were 64/4/256; page-rounded physical bytes were BF16 prefix 4,194,304, BF16 recent 16,777,216, INT2 payload 524,288, metadata 131,072. No live attention or optimization work. D2.2b PASS. Evidence: `results/oscar/phase-d2-2b-aging.md`, `results/oscar/phase-d2-2b-aging-validation.json` (SHA-256 `a702f53e4ea2d0c0120cb6d80312d25a545ba478e7a75f41830450ca00292477`) |
| E048 | Qualify mixed OSCAR cache fork, speculative mutation, rollback, commit, and StateImage restore | accepted locally | oscar/fidelity/state-transitions | Added diagnostic-only `OscarMixedTransitionCache` with immutable shared page blocks, branch-local metadata, complete logical/page/slot K/V/INT2 metadata fingerprints, COW refresh, fail-closed commit lineage, and a deterministic StateImage-compatible snapshot envelope. Forced context 324 contained prefix/historical/recent `64/4/256` and 96 page blocks across all 16 full-attention layers. Fork shared 96/96 blocks at initial refcount 2 without historical re-encoding; divergent context-328 branches each retained 16 shared prefix pages and did not corrupt the base. Four-token rollback restored the exact complete fingerprint and removed speculative positions; commit produced context 328 / tiers `64/8/256` without duplicates. Fresh restore of a context-332 image (21,760,845 bytes) reproduced all page/slot/payload/metadata hashes exactly. Existing Qwen StateImage fixed payloads remain unchanged; no Q4 shadow, live attention, or optimization work. PASS. Evidence: `results/oscar/phase-d2-2c-state-transitions.md`, validation JSON SHA-256 `df1fa525a1791cf6c6463686d1b3a741901a58825d832cca24c2e96992e16784`, executable SHA-256 `bd039908f526c8d23ecca57a27e2363b130ff7202cc938bb6a8a1c71fd285a20` |
| E049 | Prove slow mixed BF16/official OSCAR INT2 reference attention parity | accepted locally | oscar/fidelity/attention-reference | Added diagnostic-only `OscarMixedAttentionReader` with strict prefix/historical/recent page dispatch, temporary per-query BF16/INT2 row decode, FP32 Q rotation, GQA scores, stable softmax, rotated AV, and FP32 `R_V.T` recovery. An independent source-archive CPU comparator bypassed page/slot resolution and independently applied the D2.1 `OscarInt2G128` codec to historical rows. Static contexts 324/332, layer-3 boundary queries, four persistent forced appends, layers 35/63, and all 16 full-attention layers passed 31/31 comparisons; every logical trace was exactly `0..query`, and maximum rotated-Q/score/softmax/AV/recovered relative-L2 was zero under the `1e-6` gate. No GDN, legacy Q2, production fallback, serving integration, or performance work. D2.3a PASS; D2.3b may connect the qualified path to real attention. Evidence: `results/oscar/phase-d2-3a-reference-attention.md`, validation JSON SHA-256 `6090dab55f3822b283c39f306e2789d758eb351a137b09f9acdf824735015610`, executable SHA-256 `4519869eba85f900d89e4db143c5150959cfa7d7e63d0da17663064fcd8c439e` |
| E050 | Connect the qualified mixed OSCAR cache/reader to the real Qwen3.8 runtime | accepted locally | oscar/fidelity/live-runtime | Added diagnostic `oscar-int2-reference-live`: actual Q/K/V runtime tensors use C4 rotations, BF16 prefix/recent plus official `OscarInt2G128` historical storage, and the slow FP32 mixed reader. Forced sequence `997,1001,1003,1005,1007,1009,1011,1013,1015` passed independent live/reference parity for 2/2 short taps and 30/30 mixed taps; worst stage relative-L2 was zero under `2e-6`/`1e-4` fail-closed gates. Exact K/V packed payload and FP32 metadata aging parity passed 192 times across all 16 full-attention layers; dispatch bitmap `1111111111111111`, GDN and legacy-Q2 counts zero, BF16 history shadow false. Informational model drift versus BF16 is not the correctness gate. D2.3b PASS; D3 model-fidelity qualification authorized. Evidence: `results/oscar/phase-d2-3b-live-reference-runtime.md`, C4 manifest SHA-256 `c1979e86744682733a668739642ad3a945b5a6220e8b3ed983b74d33b82c3afc`, tap-set SHA-256 `9f5de2465a55ae983d0c4338657751fe10dfcccc1b35380fb75faffea374b080` |

| E051 | Determine whether calibrated OSCAR INT2 preserves minimum real-model fidelity for optimized-kernel investment | accepted locally | oscar/fidelity/model-gate | Added explicit 10K/30K runtime-bank selection and a fixed-token/natural-behavior D3.1 harness. With unchanged Qwen3.8-27B NVFP4 weights, fixed-token final-position logit relative L2 at 32/324/512 tokens was `0.023341/0.017997/0.025883` for 10K and `0.024416/0.017815/0.023566` for 30K; top-10 overlap was 9/10, 10/10, 9/10 and 8/10, 10/10, 9/10. The only fixed top-1 miss was 10K at 32 tokens with BF16 margin `0.03125` and 5/5 top-5 containment. A 12-case natural suite (3 arithmetic, 3 coding, 2 JSON, 2 retrieval, 2 copy; four retrieval/copy prompts at 1,145–1,151 tokens) achieved 12/12 objective success for BF16, 10K, and 30K. 30K is the provisional baseline based on deeper-context live results and the C4 held-out aggregate relative-L2 advantage (`0.302515` vs `0.305705`); 10K remains selectable. D3.1 PASS authorizes optimized SM120a kernel work, while 4K/16K/32K/64K/128K quality remains pending. Evidence: `results/oscar/phase-d3-1-minimum-fidelity.md`, 10K natural log SHA-256 `dc5425c24d3b6bf88a0a6bfdfcd8a685c6a12beda610e305a2d53f0923159551` |
| E052 | Profile the correct calibrated live OSCAR INT2 path and identify the first SM120a optimization target | accepted locally | oscar/performance/profile | Diagnostic-only timing at the real 30K `oscar-int2-reference-live` path completed for 512 tokens; the permitted genuine `OscarInt2G128` mixed-reader microbenchmark covered final queries at 512/2K/4K. The live 512 branch was `166.190 s` and request/verifier wall `174.529601 s`; 4K final-query reader was `213.0691 ms`, with QK `56.0506 ms`, AV `50.1997 ms`, INT2 K/V decode `34.0487/33.9776 ms`, and approximately `50.5 us` per additional historical token. D2.3a parity remained 31/31 PASS. The selected D4.2 candidate is a fused mixed-cache SM120a attention traversal; no optimization was implemented. Evidence: `results/oscar/phase-d4-1-profile.md`, live log SHA-256 `436560e22cd830993d748c51c5b03a77cf7f404f7b051991368dc1eeaca55a25`, microbench log SHA-256 `9f961107ff0e6118acbb899c1a5ff360bb1c16ff559baffeeb43419419d7d4a7` |
| E056 | Remove host full-cache staging and host recent-to-INT2 aging with a GPU-resident incremental OSCAR cache | accepted locally | oscar/performance/gpu-resident-cache | Added opt-in `oscar-int2-gpu-resident` with persistent per-full-attention-layer typed device storage, ring-aware BF16 prefix/recent publication, and device-side official `OscarInt2G128` G128 aging. Device-vs-host parity matched K/V symbols, packed bytes, and FP32 metadata exactly for `60,528` logical token checks in the 4K-plus-forced-decode validation. Live/reference taps at 321/332/512/2K/4K passed with worst recovered relative-L2 `1.40692e-6`; all 16 full-attention dispatch bits were set and GDN/Q2/BF16-shadow/fallback/full-cache-staging were zero/false. Clean resident real-model wall time was `1.505832/13.525424/46.450017 s` at 512/2K/4K versus D4.3 staged `6.889555/486.346291 s` at 512/4K; 512→4K slope fell to `12.54 ms/token`, with `4.58x`/`10.47x` improvement at 512/4K. A genuine 16K attempt was started but stopped before final measurement because the unchanged per-query launch loop remained impractical. D4.4 PASS; D4.5 should batch/fuse the per-query full-attention launch loop. Evidence: `results/oscar/phase-d4-4-gpu-resident-cache.md`, correctness log SHA-256 `29B3F15C2D6A029A8172850231CC8C924F5924AFBB4EFA4613A2A4733FB88EF8`, performance log SHA-256 `04003D44985C6C5A1D093CA87C6EF154E20FADF3ED4A76760728984A0885A3DE` |
| E057 | Make FULL OSCAR validation practical without weakening coverage or gates | accepted locally | oscar/fidelity/validation | LOCAL_MEASUREMENT: replaced the scalar tap validator with a target-local `RelWithDebInfo`/AVX2 implementation retaining fail-closed assertions, fixed row storage, GQA reuse across six Q heads, per-worker preallocation, bounded eight-way deterministic dispatch, and compact immutable snapshot replay. The unchanged 30-tap real archive passed 30/30 at the original `2e-6` relative-L2/`1e-4` max-absolute gates; external wall improved `869.854 ms` → `179.441 ms` (`4.85x`, `79.37%`). Scalar-golden validator-of-validator passed 21/21 across contexts `64/320/321/332/512/2048/4096` and layers `3/35/63`. Synthetic all-16-layer oracle benchmark: `12.6115/41.9779/88.2372 ms` at 512/2K/4K; this does not replace missing real 512/2K/4K tap archives. FULL validation remains the default and no weakened FAST path was added. Dominant arithmetic cost is QK; next candidate is CPU QK batching/fusion. Evidence: `results/oscar/full-validation-optimization.md`, source SHA-256 `BA41EFD8DAB9830BC67915369C681E1268A54C9D7CBC3A475260DB714B0BFE60`, executable SHA-256 `69A3E156C758AF08965A3E05068577048FA524CEA417022A02ED711E36C4D87A` |
